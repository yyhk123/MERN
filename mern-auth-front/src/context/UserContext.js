import { createContext } from "react";
// creates context
// context: React Context API is a way to essentially create global
//          variables that can be passed around in a React app
export default createContext(null);
